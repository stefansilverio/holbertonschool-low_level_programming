#include "holberton.h"
#include <stdio.h>

int string(char *string)
{
	int index;

	for (index = 0; string[index] != '\0'; index++)
	{
		;
	}

	printf("%d", index);
	return (index);
}

int main()
{
char 
	int count = string("cat");
	printf("%d\n", count);
	return (0);
}
