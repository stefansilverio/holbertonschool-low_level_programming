#include "holberton.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * argstostr - concatenate all arguments in program
 *
 * @ac: argument count
 *
 * @av: ptr to arguments
 *
 * Return: Always 0.
 */
char *argstostr(int ac, char **av)
{
	if ((ac == 0) || (av == NULL))
	{
		return (NULL);
	}
}
